<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="64dp"
    android:layout_marginLeft="16dp"
    android:layout_marginRight="16dp"
    android:layout_marginBottom="16dp"
    android:background="@drawable/bg_row_items"
    android:clickable="true"
    android:elevation="4dp">

    <TextView
        android:id="@+id/txt_notif_info"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginStart="48dp"
        android:layout_marginTop="22dp"
        android:text="Friend request from [User]"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <Button
        android:id="@+id/bt_accept_contact"
        android:layout_width="wrap_content"
        android:layout_height="40dp"
        android:layout_marginTop="24dp"
        android:background="@drawable/bt_push"
        android:text="Accept"
        app:backgroundTint="@null"
        app:layout_constraintStart_toStartOf="@+id/txt_notif_info"
        app:layout_constraintTop_toBottomOf="@+id/txt_notif_info" />

    <Button
        android:id="@+id/bt_decline_contact"
        android:layout_width="wrap_content"
        android:layout_height="40dp"
        android:layout_marginEnd="48dp"
        android:background="@drawable/bt_push"
        android:text="Decline"
        app:backgroundTint="@null"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toTopOf="@+id/bt_accept_contact" />
</androidx.constraintlayout.widget.ConstraintLayout>